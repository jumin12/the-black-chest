node_modules/
output/
.playwright-cli/
test-actions.json
*.txt

.vercel

# Local / production secret for F3 navigator (never commit)
navigator_admin.secret
# Ban list written by server (IPs)
bans.json
# Captain name reservations + proof tokens (do not commit)
captain_accounts.json
