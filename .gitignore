node_modules/
output/
.playwright-cli/
test-actions.json
*.txt

.vercel

# Local / production secret for F3 navigator (never commit)
navigator_admin.secret
# Ban list written by server (IPs)
bans.json
# Live notorious-pirates rankings (primary persistence; do not overwrite from git on deploy)
leaderboard.json
leaderboard.shadow.json
# Clan store written beside server.js at runtime (see server.js PARTIES_FILE shadow paths)
parties.json
parties.shadow.json
world_map.shadow.json
# Captain name reservations + proof tokens (do not commit)
captain_accounts.json
