# Sync with Render Blueprint (optional) or set Build Command in the dashboard to:
#   bash scripts/render-build.sh
services:
  - type: web
    name: the-black-chest-live
    runtime: node
    buildCommand: bash scripts/render-build.sh
    startCommand: npm start
    healthCheckPath: /health
